import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proiect-normal',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
